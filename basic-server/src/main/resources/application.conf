ktor {
    development = true
    deployment {

        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ io.github.wlara.ktor.server.examples.basic.ApplicationKt.module ]
    }
}
